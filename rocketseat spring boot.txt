[NOTION]
https://efficient-sloth-d85.notion.site/Curso-de-Java-2408d11bfc3447e980fe9460b6293976

[SPRING INITIALIZR]

https://start.spring.io/

Project - Maven
Language - Java
Spring boot - 3.0.11

Project Metadata

Group: br.com.icarus
Artifact: todolist
Name: todolist
Description: Gerenciador de tarefas
Package name: br.com.icaruas.todolist
Packaging: Jar
Java: 17

ADD DEPENDENCIES

Spring Web

- Configuração da estrutura

[https://start.spring.io/#!type=maven-project&language=java&platformVersion=3.1.4&packaging=jar&jvmVersion=17&groupId=br.com.icarus&artifactId=todolist&name=todolist&description=Gerenciador%20de%20Tarefas&packageName=br.com.icarus.todolist&dependencies=web]



* O spring boot ele tem um container e conseguir rodar a aplicação web, além de ser mais comum.


# Diferença do Jar para o War

O empacotamento via WAR requer um servidor para rodar a aplicação, enquanto o empacotamento via JAR não

# Maven

A função dele é gerenciar dependências e fazer o build.

# Controller no Spring Boot

Processar as requisições HTTP e atuar como a primeira camada de acesso do usuário à aplicação

# Forma de acessar atributos dentro de uma classe que estão como private.

Acesso através de métodos públicos na classe que retornam ou modificam o atributo.


# Definição de Private em um atributo de uma classe

O atributo pode ser acessado apenas pela própria classe

# Getters e Setters

"Getters" são usados para recuperar valores de atributos de uma classe, enquanto "setters" são usados para definir valores para esses atributos.


# Função da Annotation @RequestBody

Indica que os dados da requisição estarão no corpo da mensagem e serão convertidos para o objeto especificado no parâmetro do método.


# Lombok

É Uma biblioteca que facilita a criação de getters e setters no Java, entre outros métodos.

- Função da Annotation @Data

Facilitar a criação de getters e setters para todos os atributos de uma classe.

* Já dentro da classe java se quiser apenas colocar setters @Setters / getters @Getters / ambos @Data

# Spring Data JPA

[https://spring.io/projects/spring-data-jpa]

É uma biblioteca de persistência que facilita a comunicação com o banco de dados em uma aplicação Spring Boot.

- H2 Banco

Java SQL Database

[https://www.h2database.com/html/main.html]
